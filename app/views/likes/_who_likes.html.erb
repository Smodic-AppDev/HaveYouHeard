<% 
  song_likes = Like.all.where(song_id: a_track.id)
  leader_ids = current_user.leaders.pluck(:recipient_id)
  leader_likes = song_likes.where(user_id: leader_ids).order(created_at: "asc")              
%>
<% if leader_likes.count == 0 %>
  <small>Be the first to like this</small>
<% elsif leader_likes.count == 1 %>
  <small>
    <%= User.all.where(id: leader_likes.at(0).user_id).first.first_name %> likes this
  </small>
<% else %>
  <small>
    <%= User.all.where(id: leader_likes.at(0).user_id).first.first_name %> and
    <%= leader_likes.count - 1 %> more like this
  </small>
<% end %>            